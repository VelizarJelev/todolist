<%= form_with(model: todotask) do |form| %>
  <% if todotask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todotask.errors.count, "error") %> prohibited this todotask from being saved:</h2>

      <ul>
        <% todotask.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :task_name %>
    <%= form.text_field :task_name, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.label :duedate %>
    <%= form.date_select :duedate, class:"form-select" %>
  </div>

  <div class="actions">
    <%= form.submit class:"btn btn-secondary" %>
  </div>
<% end %>
